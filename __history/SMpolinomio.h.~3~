#include <iostream>
#include <sstream>
#include <string>
#include <tchar.h>
#include <SMemoria.h>
#pragma once
using namespace std;

class SMpolinomio {
	private:
		SMemoria& mem;
		int ptr_poli;
		int nt;
		int nulo = mem.nulo;

		int BuscarExponente(int exp) {
			int dir = ptr_poli;
			int dirEx;
			if (dir != nulo) {
				dirEx = nulo;
				while (dir != nulo && dirEx == nulo) {

                }
			}
        }
	public:

		SMpolinomio(SMemoria& M): mem(M) {};


};
